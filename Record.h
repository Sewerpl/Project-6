#ifndef RECORD_H
#define RECORD_H

#include <string>
#include <sstream>
#include <iomanip>

// Klasa reprezentująca jeden punkt pomiarowy (rekord danych)
class Record {
public:
    std::string date;  // Data w formacie YYYY-MM-DD

};

#endif // RECORD_H
